# parse convert a string to a int
strToInt = parse(Int64, "30")
println(strToInt)

strToFloat = parse(Float64, "3.1415926535")
println(strToFloat)

# trunc convert a flaoting point number to a integer
floatToInt = trunc(Int64, 5.5)
println(floatToInt)

# float convert integers to floating point numbers
intToFloat = float(30)
println(intToFloat)

# string convert integers to strings
intToStr = string(30)
println(intToStr)

